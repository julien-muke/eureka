import { SpinnerContainer, SpinnerOverlay, spinnerOverlay } from "./spinner.styles";

const Spinner = () => (
    <SpinnerOverlay>
        <spinnerOverlay />
    </SpinnerOverlay>
);

export default Spinner;